name: Shot Dashboard

on:
  push:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3
      with:
        persist-credentials: false

    - name: Set-up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm ci

    - name: Install Chinese font support
      run: |
        sudo apt-get update
        sudo apt-get install -y fonts-noto-cjk
        sudo fc-cache -f -v
    
    - name: Run node
      run: node src/index.js

    # - name: push output
    #   uses: crazy-max/ghaction-github-pages@v3.1.0
    #   with:
    #     target_branch: output
    #     build_dir: dist
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Push output
      run: |
        git config --global user.name 'WorldHim'
        git config --global user.email 'worldhim@users.noreply.github.com'
        git add output.png
        git commit -am "upd"
        git push